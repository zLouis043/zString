the allocation was not allocation the correct size (wass missing the null terminator space)

fix heap problemns on the free functions  

the upper function was not working (I fixed) 

the remove world was not working (I fixed)

the concatendStr was with alocations problems (I fixed)

the find ocurrence of was not cleaning the memory (I fixed)

the find_numbers_of_worlds was with problemns because of: isaSpace(str.data[i+1]) 
these was trying to get values of an non existent element on the end of string 